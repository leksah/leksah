# Staged/generated demo artifacts (copy-assets.sh + generators).
# demo-hovers.js is NOT ignored: regenerating it needs a dev shell with a
# working haskell-language-server (gen-demo-hovers.py), so the captured
# hover content is committed like the terminal dumps it derives from.
__pycache__/
.hover-cache.json
demo-files.js
demo-terminals.js
demo-xterm-css.js
jsexe/
leksah.js
clibs.wasm
result-js
